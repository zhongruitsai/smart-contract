# 公司治理系統功能說明文件

## 系統架構

| 層次 | 技術 | 說明 |
|------|------|------|
| 智能合約 | Solidity 0.8.20 | 部署於 Sepolia 測試網 |
| 前端框架 | Next.js 14 (App Router) | 靜態匯出，部署於 GitHub Pages |
| 區塊鏈互動 | wagmi v2 + viem | 讀寫合約、監聽事件 |
| 錢包 | MetaMask (injected connector) | 簽署交易 |

---

## 一、委託投票（Proxy Voting）

### 功能說明
股東可將投票權委託給另一位股東代為行使。委託後原股東無法自行投票，代理人可代替委託人投票。代理人投票後委託無法撤回。

### 合約層（`src/GovernanceVoting.sol`）

| 功能 | 函數 | 行號 |
|------|------|------|
| 儲存委託關係的 mapping | `proxyOf` | 第 129 行 |
| 記錄代理人是否已投票 | `proxyHasVoted` | 第 130 行 |
| 授予委託 | `grantProxy(proposalId, proxy)` | 第 298–306 行 |
| 代理人代為投票 | `voteOnBehalf(proposalId, delegator, forVotes, againstVotes, abstainVotes)` | 第 309–323 行 |
| 撤回委託（代理人未投票前） | `revokeProxy(proposalId)` | 第 326–330 行 |

**關鍵邏輯：**
- 第 304 行：一個提案只能委託一次，`ProxyAlreadyGranted` 錯誤防止重複委託
- 第 313 行：只有被委託的代理人才能呼叫 `voteOnBehalf`，否則拋出 `NotAProxy`
- 第 328 行：代理人已投票後呼叫 `revokeProxy` 會拋出 `CannotRevokeAfterProxyVoted`

### 前端層

#### `frontend/components/shareholder/ProxyPanel.tsx`（委託操作面板）

| 功能 | 行號 |
|------|------|
| 讀取目前委託對象 `proxyOf` | 第 18–24 行 |
| 讀取代理人是否已投票 `proxyHasVoted` | 第 26–33 行 |
| 送出委託交易 `grantProxy` | 第 35–43 行 |
| 撤回委託交易 `revokeProxy` | 第 45–51 行 |
| 有委託時顯示「撤回委託」按鈕 | 第 55–71 行 |
| 代理人已投票後按鈕 `disabled` | 第 62 行：`disabled={isPending \|\| !!voted}` |
| 代理人已投票後顯示說明文字 | 第 68 行 |

#### `frontend/components/shareholder/VotePanel.tsx`（投票表單）

| 功能 | 行號 |
|------|------|
| 讀取是否已授予委託 `proxyOf` | 第 29–35 行 |
| 計算是否存在有效委託 | 第 36 行：`const hasProxy = ...` |
| 已委託時隱藏投票表單 | 第 53 行：`if (hasProxy) return null;` |

#### `frontend/components/shareholder/ProxyVotePanel.tsx`（代理投票面板）

| 功能 | 行號 |
|------|------|
| 已知股東名單（用於反查委託） | 第 13–18 行 |
| 批次查詢各股東的委託對象 | 第 61–71 行 |
| 篩選出委託給目前帳號的股東 | 第 73–81 行 |
| 每位委託人的代理投票表單元件 | 第 22–65 行（`ProxyVoteForm`） |
| 讀取委託人快照持股數 | 第 29–31 行 |
| 送出 `voteOnBehalf` 交易 | 第 39–49 行 |
| 顯示委託人名稱與持股數 | 第 53 行 |

---

## 二、聯署提案（Cosign Proposal）

### 功能說明
股東可發起「聯署提案」，需集滿 10 位不同股東連署，且所有連署人合計持股達總股份 1% 以上，提案才正式生效。連署截止日等同提案期截止日。

### 合約層（`src/GovernanceVoting.sol`）

| 功能 | 函數／欄位 | 行號 |
|------|-----------|------|
| 提案結構中聯署截止日 | `cosignDeadline` | 第 45 行 |
| 提案結構中連署人數 | `cosignerCount` | 第 46 行 |
| 提案是否已生效 | `isActive` | 第 47 行 |
| 記錄誰已連署的 mapping | `hasCosigned` | 第 128 行 |
| 連署人清單（私有） | `_cosignerList` | 第 133 行 |
| 建立聯署提案事件 | `CosignProposalCreated` | 第 93 行 |
| 連署動作事件 | `ProposalCosigned` | 第 94 行 |
| 執行連署 | `cosign(proposalId)` | 第 249–267 行 |
| 達 10 人後提案生效 | `isActive = true` | 第 262 行 |
| 連署截止日設定 | `cosignDeadline = proposingDeadline` | 第 238 行 |
| 連署人持股合計需 ≥ 1% 驗證 | `_checkCosignerBalance` | 第 355–365 行 |

**關鍵邏輯：**
- 第 252 行：連署截止後呼叫 `cosign` 會拋出 `CosignDeadlinePassed`
- 第 253 行：同一人不能連署兩次，拋出 `AlreadyCosigned`
- 第 260–263 行：第 10 位連署人成功後，自動呼叫持股驗證並將 `isActive` 設為 `true`

### 前端層

#### `frontend/components/shareholder/CosignList.tsx`

| 功能 | 行號 |
|------|------|
| 讀取目前帳號是否已連署 `hasCosigned` | 第 16–22 行 |
| 讀取合約時間（用於判斷截止） | 第 24–30 行 |
| 計算連署是否仍開放 | 第 32 行：`const cosignOpen = ...` |
| 送出連署交易 `cosign` | 第 34–39 行 |
| 顯示連署進度 X/10 | 第 47 行 |
| 顯示「我要聯署」按鈕條件 | 第 49 行：未生效 且 未截止 且 未連署 |
| 截止後未達門檻顯示失敗訊息 | 第 54 行 |
| 已達 10 人顯示生效訊息 | 第 56 行 |

#### `frontend/components/shareholder/ProposalList.tsx`（整合顯示）

| 功能 | 行號 |
|------|------|
| 提案狀態顯示「聯署中 X/10」 | 第 83 行 |
| 條件渲染 `CosignList` 元件 | 第 119 行：`{proposal.isCosignProposal && <CosignList .../>}` |

---

## 三、時間快轉（Time Warp）

### 功能說明
為了 Demo 方便，管理員可在合約層面快轉時間，模擬提案期、投票期截止等流程，而不需要真的等待數週。

### 合約層

兩個合約（`GovernanceVoting.sol`、`DirectorElection.sol`）都有相同的時間偏移機制：

| 功能 | 行號（兩合約相同） |
|------|------|
| 儲存時間偏移量 | `uint256 public timeOffset` |
| 取得目前合約時間 | `function _now()` → `block.timestamp + timeOffset` |
| 對外查詢合約時間 | `function currentTime()` |
| 累加時間偏移（僅管理員） | `function addTimeOffset(uint256 secs)` |

**GovernanceVoting.sol** 對應行號：第 119–123 行
**DirectorElection.sol** 對應行號：第 86–90 行

### 前端層（`frontend/components/TimeController.tsx`）

| 功能 | 行號 |
|------|------|
| 讀取合約目前時間 | 第 51–57 行 |
| 本地網路（Anvil）快轉 | 第 13–24 行：呼叫 `evm_increaseTime` + `evm_mine` |
| Sepolia 快轉 | 第 68–79 行：呼叫兩個合約的 `addTimeOffset` |
| 預設快轉按鈕（+1天 到 +60天） | 第 34–42 行 |
| 自訂天數輸入 | 第 90–94 行 |

---

## 四、股東持股動態掃描

### 功能說明
持股總覽不再依賴硬寫的固定帳號清單，而是掃描鏈上 `Transfer` 事件自動發現所有曾持股的地址。

### 前端層（`frontend/components/admin/ShareholderRegistry.tsx`）

| 功能 | 行號 |
|------|------|
| 已知帳號名稱對照表 | 第 13–18 行 |
| Transfer 事件定義 | 第 20–22 行：`parseAbiItem(...)` |
| 掃描最近 50,000 個區塊的 Transfer 事件 | 第 30–50 行 |
| 合併事件地址與已知地址 | 第 41–43 行 |
| 掃描失敗時退回已知清單 | 第 45–46 行 |
| 批次讀取各地址餘額 | 第 53–68 行 |
| 過濾餘額為 0 的地址 | 第 82 行：`.filter(r => r.balance > 0n)` |

---

## 五、提案列表排序

### 功能說明
提案列表改為最新提案優先顯示（降序），方便快速查閱最新狀態。

### 前端層（`frontend/components/shareholder/ProposalList.tsx`）

| 功能 | 行號 |
|------|------|
| 降序渲染提案（最新在前） | 第 174 行：`Array.from({ length: count }, (_, i) => count - 1 - i)` |

---

## 六、投票狀態同步（使用合約時間）

### 功能說明
前端判斷「投票是否開放」時，改用合約的 `currentTime()`（含時間偏移），確保快轉後 UI 狀態與合約狀態一致。

### 前端層

| 檔案 | 修改說明 | 行號 |
|------|---------|------|
| `ProposalList.tsx` | 改用 `currentTime()` 讀合約時間 | 第 32–38 行 |
| `ProposalList.tsx` | 用合約時間判斷 `votingOpen` / `canFinalize` | 第 67–69 行 |
| `CosignList.tsx` | 改用 `currentTime()` 判斷聯署截止 | 第 24–32 行 |
